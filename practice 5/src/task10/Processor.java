package task10;

// Для простоты компоненты описаны прямо здесь
class Processor { String model; public Processor(String model) {this.model = model;} }
class Memory { int ram; public Memory(int ram) {this.ram = ram;} }
class Monitor { double diagonal; public Monitor(double d) {this.diagonal = d;} }
